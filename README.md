# tallycraft.app

The public website for the TallyCraft iOS app — the support page and privacy policy, served by GitHub Pages at https://tallycraft.app.

The app itself lives in the private `TallyCraft` repo; the canonical policy draft is `docs/ship-prep/` there. Keep the two in sync when the policy changes.
